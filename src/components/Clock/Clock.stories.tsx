import React, {useEffect, useMemo, useState} from "react";
import {Clock} from "./Clock";

export default {
    title: "Clock",
    component: Clock
}

export const BaseExample1 = () => {
    return <Clock />
}
